# MD2H
Convert Markdown files to HTML.
## Steps
- Download `md2h.exe`.
- Add the folder containing `md2h.exe` to the PATH.
- From the command line, run:
  ```
  md2h .
  ```
  This searches all child directories for `.md` files and converts them to `.html`.
